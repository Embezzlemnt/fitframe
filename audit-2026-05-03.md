# FitFrame AI Visibility Audit - 2026-05-03

## Deployment
Cloudflare Worker deploy completed successfully.

Version ID: `741429a3-baec-42aa-a754-d803dc3cd548`

Production root: `https://fitframe.store/`

## Build Verification
`npm run build` passed.

`eslint .` passed.

The production `dist/` folder contains:

- `index.html`
- `llms.txt`
- `robots.txt`
- `_headers`
- `about.html`
- `about/index.html`
- `4a773f0e99cb4f1f9bd0869ff1f6d7c2.txt`

## Live Fetch Verification
`curl -I https://fitframe.store/` returned `200 OK`.

The live root response includes:

- `Cache-Control: no-store`
- `X-Frame-Options: DENY`
- `X-Content-Type-Options: nosniff`
- `Referrer-Policy: strict-origin-when-cross-origin`
- `Permissions-Policy: camera=(self), microphone=(), geolocation=()`

`curl -A "Mozilla/5.0" https://fitframe.store/llms.txt` returned `200 OK` and the full eight-section `llms.txt` content.

`curl -I https://fitframe.store/about` returned `200 OK`.

`curl -I https://fitframe.store/4a773f0e99cb4f1f9bd0869ff1f6d7c2.txt` returned `200 OK`.

## Structured Data Verification
The live `index.html` contains a static `<script id="fitframe-json-ld" type="application/ld+json">` in the document head.

Local and production JSON parsing confirmed these schema types:

- `Product`
- `WebSite`
- `Organization`
- `FAQPage`

The live FAQPage schema contains 8 questions.

The live Organization schema contains:

- `name: FitFrame`
- `founder.name: Lorenzo`
- `addressRegion: PA`
- `addressCountry: US`
- `email: hello@fitframe.store`
- `url: https://fitframe.store`
- `sameAs: https://fitframe.store`
- `sameAs: https://github.com/Embezzlemnt/fitframe`

The live canonical tag is present:

`<link rel="canonical" href="https://fitframe.store" />`

## Indexing
IndexNow root GET submission returned `202 Accepted`.

IndexNow batch POST submission returned `200 OK` for:

- `https://fitframe.store/`
- `https://fitframe.store/about`
- `https://fitframe.store/about.html`
- `https://fitframe.store/llms.txt`

Google Search Console URL inspection and Bing Webmaster Tools submission require verified owner dashboard access. They were not completed from this environment.

## External Validator Status
The production JSON-LD is valid JSON and parses locally with the expected schema types.

`validator.schema.org` could not be completed through curl because the request returned a Google `405 Bad Request` page.

Google Rich Results Test loaded the Search Console app shell through curl but did not return a machine-readable validation result.

Manual browser verification is still required for:

- `https://validator.schema.org`
- `https://search.google.com/test/rich-results`

## Cloudflare Bot Management
Cloudflare Bot Management was investigated at the task level but not configured from this environment because it requires Cloudflare dashboard or account-scoped API access.

Recommended dashboard path:

Cloudflare Dashboard -> Security -> Bots

Recommended policy:

- Allow verified AI and search crawlers such as GPTBot, ChatGPT-User, ClaudeBot, anthropic-ai, PerplexityBot, and Google-Extended when available in Cloudflare's verified bot controls.
- Challenge suspicious non-verified automation.
- Keep public marketing files crawlable: `/`, `/about`, `/about.html`, `/llms.txt`, and `/robots.txt`.

## Remaining Manual Owner Tasks
Submit `https://fitframe.store/` and `https://fitframe.store/llms.txt` in Google Search Console.

Submit `https://fitframe.store/` and `https://fitframe.store/llms.txt` in Bing Webmaster Tools.

Run the two browser validators and record screenshots or pass/fail notes in the next audit.
